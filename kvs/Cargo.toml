[package]
name = "kvs"
version = "0.1.0"
authors = ["唐盛 <tangsheng02@meituan.com>"]
description = "A key-value store"
edition = "2018"

[dependencies]
clap = {version = "2.33", features = ["yaml"]}

[dev-dependencies]
assert_cmd = "0.11.0"
predicates = "1.0.0"
